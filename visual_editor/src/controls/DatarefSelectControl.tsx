// controls/DatarefSelectControl.tsx
import { ClassicPreset } from "rete";

export class DatarefSelectControl extends ClassicPreset.Control {
    // the key is optional; we’ll just use it to identify the control type
    constructor() { super(); }
}